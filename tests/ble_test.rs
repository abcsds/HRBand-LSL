use hrband_lsl::ble::BleDeviceManager;

#[test]
fn test_ble_device_manager_module_exists() {
    // This test verifies that the BleDeviceManager struct exists
    // and can be referenced. On platforms without BLE hardware,
    // we can't test the actual scanning functionality.
}

#[test]
fn test_constants_defined() {
    // Test that the module compiles with the required constants
    // These are tested indirectly through the module's compilation
}
